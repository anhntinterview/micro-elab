import * as React from 'react';

interface ITipTapEditorProps {
}

const TipTapEditor: React.FunctionComponent<ITipTapEditorProps> = (props) => {
  return <div className="">
    
  </div>;
};

export default TipTapEditor;
